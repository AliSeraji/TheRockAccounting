import type { ReactNode } from 'react';

export default function Customers(): ReactNode {
  return (
    <div className="w-full h-full justify-center text-center align-middle">
      Customers
    </div>
  );
}
